#include <iostream>
#include <string>
using std::cin, std::cout, std::endl, std::string;

int main() {
    /*
    string equation;
    cout << "Please enter word equation (type 'equals' at the end): ";
    getline(cin, equation);
    while (equation.find("one") != string::npos){
        equation.replace(equation.find("one"), 3, "1");
    }

    while (equation.find("two") != string::npos){
        equation.replace(equation.find("two"), 3, "2");
    }
    while (equation.find("three") != string::npos){
        equation.replace(equation.find("three"), 5, "3");
    }
    while (equation.find("four") != string::npos){
        equation.replace(equation.find("four"), 4, "4");
    }
    while (equation.find("five") != string::npos){
        equation.replace(equation.find("five"), 4, "5");
    }
    while (equation.find("six") != string::npos){
        equation.replace(equation.find("six"), 3, "6");
    }
    while (equation.find("seven") != string::npos){
        equation.replace(equation.find("seven"), 5, "7");
    }
    while (equation.find("eight") != string::npos){
        equation.replace(equation.find("eight"), 5, "8");
    }
    while (equation.find("times") != string::npos){
        equation.replace(equation.find("times"), 5, "*");
    }
    while (equation.find("plus") != string::npos){
        equation.replace(equation.find("plus"), 4, "+");
    }
    while (equation.find("divides") != string::npos){
        equation.replace(equation.find("divides"), 7, "/");
    }
    while (equation.find("equals") != string::npos){
        equation.replace(equation.find("equals"), 7, "=");
    }
    int num[equation.length()];
    int op[equation.length()];
    for (int i=0; i<equation.length();i++) {
        if (isdigit(int(equation[i]))) {
            num[i] = equation[i];
        }
        else {
            op[i] = equation[i];
        }
    }


    cout << equation;
    cout << num[0] << endl;
    cout << op[0] << endl;*/
    return 0;
}
